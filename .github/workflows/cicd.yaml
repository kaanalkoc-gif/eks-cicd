name: cicd

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  issues: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}
  AWS_REGION: us-east-1
  EKS_CLUSTER_NAME: yeni-clusterim

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Repository kodunu al
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2. SAST - SonarQube
      - name: SonarQube SAST Scan
        uses: SonarSource/sonarqube-scan-action@v7
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      # 3. GitHub Container Registry'ye giriş
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 4. Docker image oluştur
      - name: Build Docker image
        run: |
          docker build -t $IMAGE_NAME:${{ github.sha }} .

      # 5. Container'ı GitHub runner üzerinde test et
      - name: Test container
        run: |
          docker run -d --name test-app -p 8000:8000 $IMAGE_NAME:${{ github.sha }}
          sleep 5
          curl --fail http://localhost:8000/
          curl --fail http://localhost:8000/health
          docker stop test-app

      # 6. Image'ı GHCR'a gönder
      - name: Push Docker image
        run: |
          docker push $IMAGE_NAME:${{ github.sha }}

      # 7. AWS'ye bağlan
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # 8. AWS bağlantısını doğrula
      - name: Verify AWS identity
        run: |
          aws sts get-caller-identity

      # 9. EKS bağlantısını oluştur
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region "$AWS_REGION" \
            --name "$EKS_CLUSTER_NAME"

      # 10. Deployment manifestine yeni Docker image'ı yerleştir
      - name: Update Kubernetes image
        run: |
          sed -i \
            "s|IMAGE_PLACEHOLDER|$IMAGE_NAME:${{ github.sha }}|g" \
            k8/deployment.yaml

      # 11. Kubernetes manifestlerini EKS'e uygula
      - name: Deploy to EKS
        run: |
          kubectl apply -f k8/

      # 12. Yeni deployment'ın tamamlanmasını bekle
      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/dummy-microservice --timeout=180s

      # 13. LoadBalancer hostname oluşmasını bekle
      - name: Wait for LoadBalancer hostname
        run: |
          echo "Waiting for LoadBalancer hostname..."

          for i in {1..30}; do
            LB_HOST=$(kubectl get svc dummy-microservice-service \
              -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')

            if [ -n "$LB_HOST" ]; then
              echo "LoadBalancer hostname: $LB_HOST"
              echo "APP_URL=http://$LB_HOST" >> "$GITHUB_ENV"
              exit 0
            fi

            echo "LoadBalancer hostname not available yet..."
            sleep 10
          done

          echo "LoadBalancer hostname was not created in time."
          kubectl get svc
          exit 1

      # 14. LoadBalancer DNS kaydının aktif olmasını bekle
      - name: Wait for LoadBalancer DNS
        run: |
          echo "Waiting for DNS: $APP_URL"

          LB_HOST="${APP_URL#http://}"

          for i in {1..30}; do
            if getent hosts "$LB_HOST" > /dev/null 2>&1; then
              echo "DNS is ready."
              getent hosts "$LB_HOST"
              exit 0
            fi

            echo "DNS not ready yet..."
            sleep 10
          done

          echo "LoadBalancer DNS could not be resolved."
          exit 1

      # 15. Uygulamanın stabil şekilde erişilebilir olduğunu doğrula
      - name: Verify deployed application
        run: |
          echo "Testing $APP_URL"

          SUCCESS=0

          for i in {1..20}; do
            if curl \
              --fail \
              --connect-timeout 5 \
              --max-time 10 \
              "$APP_URL/health"; then

              SUCCESS=$((SUCCESS + 1))
              echo ""
              echo "Successful health check: $SUCCESS/3"

              if [ "$SUCCESS" -ge 3 ]; then
                echo "Application is stable and ready for DAST."
                exit 0
              fi
            else
              SUCCESS=0
            fi

            sleep 10
          done

          echo "Application is not stable enough for DAST."
          exit 1

      # 16. DAST - çalışan uygulamayı OWASP ZAP ile tara
      - name: OWASP ZAP Baseline DAST
        uses: zaproxy/action-baseline@v0.15.0
        with:
          target: ${{ env.APP_URL }}

      # 17. Pipeline sonucu ne olursa olsun Kubernetes kaynaklarını göster
      - name: Show Kubernetes resources
        if: always()
        run: |
          kubectl get deployment dummy-microservice -o wide
          kubectl get pods -o wide
          kubectl get services
